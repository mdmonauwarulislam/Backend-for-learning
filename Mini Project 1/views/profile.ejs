<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Project</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="bg-zinc-900 w-full min-h-screen text-white p-10">
        <a class="bg-red-300 rounded-md py-2 px-2 float-right ml-16 block mt-5" href="/logout">Logout</a>
        <h1 class="text-3xl mb-5">User Profile</h1>
        <h1 class="text-3xl mb-5"><span class="font-medium text-blue-500">Hello</span>, <%= user.name %> </h1>
        <h5 class="text-[12px] mt-2 ">You can create a new post </h5>

        <form method="post" action="/post">
            <textarea class="resize-none w-1/3 p-3 bg-transparent border-2 border-white mt-2 rounded-md" placeholder="What's is in your mind?" name="content"></textarea>
            <input type="Submit" class="py-2 px-3 rounded-md bg-blue-500 mt-2 block w-40 cursor-pointer" value="Create Post">
        </form>

        <div class="posts mt-20">
            <h3 class=" text-2xl text-zinc-300">All Posts</h3>
            <div class="postcontainer">
                
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post rounded-md w-1/3 border-2 border-zinc-800 p-4 mt-5">
                      <h4 class="text-xl text-blue-500">@<%= user.username %></h4>
                      <p class="mt-1 tracking-tight"><%= post.content %></p>
                      <small><%= post.likes.length %> likes</small>
                      <div class="btns flex gap-4 mt-5">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                        </a>
                        <a class="text-zinc-300" href="/edit/<%= post._id %>">Edit</a>
                      </div>
                    </div>
                  <% }) %>
                  
                
                
            </div>
        </div>
       
    </div>
</body>
</html>